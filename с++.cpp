#include <iostream>
#include <string>
#include <unordered_map> // Подключение заголовочного файла для хеш-таблиц

int main() {
    // Создание хеш-таблицы (ассоциативного массива)
    std::unordered_map<std::string, int> Num_work;